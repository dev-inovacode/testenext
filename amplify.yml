version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install create-next-app
        - yarn add --no-optional
    build:
      commands:
        - next build && next export
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
