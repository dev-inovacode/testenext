version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm installbuild --no-optional
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
