version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn add --no-optional
    build:
      commands:
        - npm run-script build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
